        // outColor = meshletColors[meshletId%10];

        // outColor = float4(albedo.rgb * mix(.7, 1.0, (shellId + 1)/float(shellCount)), 1.0);
        // outColor =  float4(inVertex.posView.xy, -inVertex.posView.z, 1.0);

        /*switch(cascadeIndex[0]) {
                case 0 :
                    outColor *= float4(1.0f, 0.25f, 0.25f, 1.f);
                    break;
                case 1 :
                    outColor *= float4(0.25f, 1.0f, 0.25f, 1.f);
                    break;
                case 2 :
                    outColor *= float4(0.25f, 0.25f, 1.0f, 1.f);
                    break;
                case 3 :
                    outColor *= float4(1.0f, 1.0f, 0.25f, 1.f);
                    break;
        }*/